{
  "scripts": {
    "build:sass": "gulp build-sass",
    "watch:sass": "gulp develop",
    "watch:js": "webpack --watch --mode development",
    "build:js": "webpack --mode production",
    "build": "npm run build:sass && npm run build:js",
    "live": "live-server dist/"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-sass": "^4.1.0",
    "live-server": "^1.2.1",
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "rangeslider-js": "^3.2.5"
  }
}
